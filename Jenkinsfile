pipeline {
	agent any
	stages {
		stage('Init') {
			steps {
				echo 'Hi All Prakarsh this side'
			}
		}
		stage('Build') {
			steps {
				echo 'Build stage is here'
			}
		}
		stage('Deploy') {
			steps {
				echo 'Deploying it to tomcat'
			}
		}
	}
}
